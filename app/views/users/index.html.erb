<h3>Users</h3>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, edit_user_path(user) %> signed up <%= user.created_at.to_date %>
    </li>
  <% end %>
</ul>

<%= link_to 'New User', new_user_path %>
